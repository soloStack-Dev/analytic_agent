"""Shared visual constants for Matplotlib chart renderers."""

# Keeping the palette in a neutral module prevents renderers from importing the
# generator that imports those same renderers.
PALETTE = [
    "#4f46e5",
    "#0ea5e9",
    "#10b981",
    "#f59e0b",
    "#ef4444",
    "#8b5cf6",
    "#14b8a6",
]